version: '3.8'

services:
  web:
    image: nginx:alpine
    ports:
      - "8083:80"
    volumes:
      - ./html:/usr/share/nginx/html

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
